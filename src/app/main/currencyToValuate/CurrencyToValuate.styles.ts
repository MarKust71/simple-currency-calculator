import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  inputWrapper: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
}));
